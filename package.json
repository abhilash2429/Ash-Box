{
  "name": "executor",
  "version": "1.0.0",
  "description": "Ephemeral local code execution tool",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build-image": "node build-image.js"
  },
  "keywords": ["electron", "docker", "code-runner"],
  "license": "MIT",
  "dependencies": {
    "dockerode": "^4.0.0"
  },
  "devDependencies": {
    "electron": "^28.0.0"
  }
}
