# Extension Workflow

1. Build and run bridge (`npm run bridge`).
2. Load `extension/` via Chrome developer mode.
3. Open side panel with toolbar action or shortcut.
4. Run code and inspect console output.
