# Testing Notes

Current repository does not include a formal test suite.

Recommended smoke tests:

1. Run desktop app (`npm start`).
2. Run bridge (`npm run bridge`).
3. Load extension and execute sample code in at least one language.
4. Confirm runtime status indicators update correctly.
