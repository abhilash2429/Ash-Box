# Coding Standards

- Keep files in module directories (`desktop/`, `runtime/`, `extension/`, `scripts/`).
- Prefer small, focused functions.
- Keep side effects explicit.
- Use clear commit messages with scope.
